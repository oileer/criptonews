# Status — sprint de 21/07/2026 (criptonews + e-trade.ai)

O que foi feito hoje, o que está pendente e exatamente onde retomar. Pra qualquer agente (ou o Euller) continuar sem re-descobrir nada.

---

## ✅ Feito e em produção

### criptonews (commit `dee6bbf`)
- **`run-daily.sh` endurecido** — status por etapa em `robo.status`, `git pull --rebase --autostash`, falha não trava mais em silêncio.
- **Watchdog** (`scripts/watchdog.mjs`) — cron a cada 10min no kody; checa edição do dia (até 6h20) e frescor do snapshot da API (<30min); alerta por Resend com dedupe diário. Log: `~/apps/criptonews/watchdog.log`.
- **RSS** — `/rss.xml` (30 últimas edições PT), linkado no `<head>`. Pré-requisito do Google News/Bing.
- **`/hoje`** — permalink que redireciona pra edição mais recente (bio, QR, grupos).

### e-trade.ai (commits `7705557` → `58ea1ec`)
- **Concessões pendentes** — `/api/admin/conceder-creditos` pra e-mail que nunca logou grava `concessoesPendentes/{email}`; `/api/creditos` auto-aplica no 1º login via `ativarPlanoHH`. **Igor (igormeneghini@icloud.com) já registrado: 10/dia, ~10 anos.**
- **Analyser cirúrgico** — schema devolve: ativo, timeframe, direção (long/short/neutro), níveis numéricos (entradaNum/stopNum/alvosNum), `tempoEstimado {horizonte, porque}`. Front mostra badges no card e no histórico.
- **Contexto ao vivo** — kody empurra snapshot (termômetro, F&G, funding, ~46 preços do top 50 sem stables) a cada 15min pra `/api/market/snapshot` (secret `MARKET_SNAPSHOT_SECRET`, já na Vercel e no kody). `runAnalise` injeta no prompt; avisa se o print parece desatualizado. Fail-open se snapshot >60min.
- **Tracking TP/stop** — análise rastreável vira doc em `ordensAbertas`; `scripts/verificar-ordens.mjs` (cron 15min no kody, `~/apps/etrade-tools`) confere candles 15m da Binance e marca qual TP ou stop bateu (stop+TP no mesmo candle = stop; expira em 14 dias). Badge de status no histórico.
- **`/v1/market/context/:ativo`** na etrade-api — preço/high/low/volume ao vivo, funding do perp, volatilidade 24h, ATR% diário 14p, rank/mcap, leitura geral. Normaliza entrada solta (`btc`, `BTC/USDT`, `BTCUSDT.P`), cache 5min. Repo sincronizado com o drift do kody (auth x-api-key + haiku).

### Funil / negócio
- **Postback HashHedge CONFIGURADO** no painel (21/07). Rota testada respondendo 401 pra secret errada. Falta só validar com a primeira compra real (ou simulação).

### Infra kody — crontab atual
```
@reboot pm2 resurrect
0 6 * * *    run-daily.sh (robô criptonews)
*/10 * * * * watchdog.mjs (criptonews)
*/15 * * * * push-snapshot.mjs (etrade-tools)
*/15 * * * * verificar-ordens.mjs (etrade-tools)
```
`~/apps/etrade-tools/.env` tem: ETRADE_API_KEY, MARKET_SNAPSHOT_SECRET, FIREBASE_ADMIN_KEY.

### Meta / redes sociais
- App **Kody-IG** criado (caso de uso "API do Instagram com Login do Instagram" — NÃO precisa de Página do FB).
- Conta `@cripto_etradeai` (Creator) como testadora, **token gerado e VALIDADO** via `graph.instagram.com/me`.
- Credenciais já no kody: `IG_ACCESS_TOKEN`, `IG_USER_ID=27964684416459505`, `IG_APP_ID`, `IG_APP_SECRET` em `~/apps/criptonews/.env.local`. (Cópia local: `Desktop/backup-kody/instagram api.env` — apagar depois que tudo rodar.)

---

## ⏸ Pendente — retomar daqui

1. **Threads (PRÓXIMO PASSO — Euller em casa):** deu erro no fluxo OAuth (diagnóstico pendente — conferir Threads App ID correto, redirect URI idêntica salva no produto, convite de testadora aceito NO APP do Threads). Entregar: Threads App ID + App Secret + `code` do OAuth (expira em minutos — trocar por token na hora).
2. **`post-social.mjs`** — construir assim que o Threads destravar (IG feed + story via `graph.instagram.com`, Threads texto+imagem via `graph.threads.net`, renovação automática dos dois tokens no cron). O `run-daily.sh` já chama o script se ele existir.
3. **Análise em 2 passos** (aprovada, não iniciada): detectar ativo → puxar `/v1/market/context/{ativo}` completo (funding/ATR específicos) → prompt final. Dobra o custo pra ~US$0,03/análise.
4. **Rotação de credenciais** (Euller): Firebase service account + key Anthropic → atualizar `.env.local` locais e Vercel → aí a IA propaga pro kody e reinicia.
5. **Registros SEO** (Euller): Search Console (enviar `sitemap.xml` + `rss.xml`), Publisher Center (feed RSS), Bing Webmaster (importar do GSC).
6. **Backlog semana 2-3** (SUGESTOES-CRIPTONEWS-ETRADEAI.md): `/bitcoin-hoje`, termômetro ao vivo nas homes (o `GET /api/market/snapshot` público já serve — dispensa o Cloudflare Worker), página de venda do relatório semanal, card de compartilhamento por análise, demo sem login, unsubscribe real + List-Unsubscribe.

## Custo de tudo isso
Infra ~R$0 (free tiers + kody). Variável só Anthropic: newsletter ~US$1/mês; análise de print ~US$0,015 cada (Haiku). Primeiro gargalo pago: Resend >3k e-mails/mês.
